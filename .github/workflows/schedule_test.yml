name: Schedule Test

on:
  workflow_dispatch:
  push:
    branches:
      - main
  schedule:
    - cron: "7/15 * * * *"

jobs:
  test:
    runs-on: ubuntu-latest

    steps:
      - name: Show schedule test
        run: |
          echo "Scheduled workflow is working"
          date -u
